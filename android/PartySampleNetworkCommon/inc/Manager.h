#pragma once

// Base class for persistent controller objects to be held in the managers class.

namespace PartySample
{

    class Manager
    {
    public:
        Manager() = default;
        virtual ~Manager() = default;
    };

}

